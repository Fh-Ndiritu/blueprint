<div class="flex flex-col justify-between w-full items-center">
  <h1 class="font-bold text-4xl">This is home :)</h1>
  <div class="flex">

    <% if user_signed_in? %>
      <%= link_to "Log out", destroy_user_session_path, class: "btn", data: { turbo_method: :delete, turbo_frame: '_top' } %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: "btn btn-default"  %>
    <% end %>

  </div>
</div>
